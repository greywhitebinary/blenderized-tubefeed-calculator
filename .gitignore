# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Data we generate (keep raw CNF, ignore processed parquet)
data/processed/
data/raw/
*.parquet

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Streamlit
.streamlit/secrets.toml

# Generated/exported documents at repo root (regenerable from BUSINESS_CASE.md;
# root-anchored so this does NOT touch PDFs under cnf_fcen_all-files-data_2026/)
/BTF Calculator.pdf
/BTF_Calculator_Complete.*
/BUSINESS_CASE.epub

# Local-only drafts (Skool posts, video scripts, etc.) — not for the repo
/drafts/

# Secrets / env (defensive — none in repo now, but never let one land)
.env
.env.*

# Testing / lint / coverage caches (Week 3: pytest, ruff, coverage)
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
